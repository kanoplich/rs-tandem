# Meeting Notes #6 - AI Judge создать моки и Edge Function

**Дата:** 25 февраля 2026

**Длительность:** ~3 часа

**Цель:** Создать API слой для работы AI Judge согласно документации проекта.

Разобрались с главной идеей работы AI на проекте: 3 уровня Judge — Level 0 (мок, клиент) → Level 1 (LLM через Edge Function) → Level 2 (продвинутый)

Ключевой принцип: сначала UI работает на моках, потом подключаем реальный AI

### Разбор Data Contracts

Добавили интерфейсы, методов, мок для`JudgeResult` в `shared/api/judge`

### Level 0

Настроили env переменных. Добавили логику с Level 0

- `evaluateLevel0` — keyword matching по `rubricItems`
- это работает на **клиенте** и не требует Edge Function
- **Переключатель через env:** `VITE_JUDGE_LEVEL=0` → мок, `=1` → Edge Function

### Edge Function: работа с Level1

**Цель:** разобрались с архитектурой serverless функции, почему теперь енв ключ по AI API в безопасности.

Разобрали код из ai-judge.md Level 1:

- Deno Runtime в Supabase Cloud
- Supabase клиент: `SUPABASE_SERVICE_ROLE_KEY` (читает `golden_answer`) ( запиcывает submission от имени пользователя)
- Системный промпт для Groq - структура, возможности , как с ним работать дальше (варианты , разные роли, ограничения, примеры)
- `supabase.functions.invoke("judge", { body: { taskId, answer } })` — как вызывать с клиента
- Установили секреты: `supabase secrets set GROQ_API_KEY=gsk_...`
- Сделали деплой: `supabase functions deploy judge`
- Переключили в `.env`: `VITE_JUDGE_LEVEL=1`
- после каждого изменения `supabase.function`нужно запускать деплой на supabase.

### Что необходимо доделать по Level1

1. Добавить обработку ошибок: `try/catch` вокруг `supabase.functions.invoke`, (возможно toast-уведомления для клиента? тут вопрос)
2. Добавить `AbortController` — отменять запрос если пользователь ушёл со страницы (упоминается в документации как типичная ошибка)
3. Необходимо разобраться с полииками Supabase, почему сейчас не выполняется запрос. Протестировать чтобы сама функция выполнялась и шел запрос к АПИ Gtoq.
4. Как только появтяся данные в таблицах топиков и заданий, можно будет работать над шлифовкой системного запроса, и тестировать полученне результаты.
5. Продолжить изучение требований, возможно еще что-то не учли.

# Дата: 2026-03-23

- **Что было сделано:**

Я приступил к реализации страницы Dashboard. Для начала я ознакомился с тем, как в нашей команде создается кастомный хук, необходимый для загрузки всех требуемых компонентов, в котором делается запрос и вывод данных, а также ошибок и лоадера.

Далее я создал заголовок и описание, и приступил к созданию трех карточек с общей статистикой. В результате мне пришлось познакомиться с тем, как работают функции getDashboardStats и getTopicProgress, а также вспомогательные функции formatScore() для перевода оценки в десятичную систему и isTopicCompleted() для проверки завершенных тем.

- **Проблемы, с которыми я столкнулся:**

Проблемы были в основном связаны с нехваткой времени на разработку. В создании страницы проблем не было.

- **Планы:**

В ближайшее время я планирую приступить к разработке UI каждой карточки, кнопки-ссылки для перехода на страницу тем и StageCards.

- **Затраченное время:** 3 часа

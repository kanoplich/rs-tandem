# Дата: 2026-03-07

- **Что было сделано:**

Я целиком реализовал Edge Function, добавив все необходимые проверки:

- Проверку входных данных (taskId, answer, Authorization header)
- Проверку авторизации (supabase.auth.getUser())
- Проверку получения задачи из таблицы
- Безопасный парсинг ответа LLM
- Helper-функции для валидации (isStringArray, isValidJudgeResponse)
- Проверку сохранения результата в submissions
- Единый формат HTTP-ответов

Также я улучшил промпт для LLM и добавил защиту от:

- prompt injection
- попыток изменить формат ответа
- попыток игнорировать rubric

- **Проблемы, с которыми я столкнулся:**

В данной работе проблем не возникло.

- **Планы:**

В ближайшее время я планирую проверить работу функции на реальных тестовых задачах, добавить стриминг ответа LLM и таймаут для запроса к Groq, чтобы Edge Function не зависала.

- **Затраченное время:** 6 часов

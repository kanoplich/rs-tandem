# Дата: 2026-03-16

- **Что было сделано:**

Я проделал код ревью одного из членов нашей команды. Поскольку в нашем проекте я работаю с AI-компонентом, мне было необходимо понять, как работает связь с сервером, как создаются таблицы, как обновляются данные, и для этого я много разбирался с тем, как работает Supabase, изучал примеры кода для бэкэнда, предоставленные школой, и анализировал ПР другого разработчика, который тоже работает с бкэндом.

- **Проблемы, с которыми я столкнулся:**

Основной проблемой было то, что я должен был научиться разбираться в коде, который до этого не изучал. Поэтому пришлось подолгу общаться с ИИ и просить объяснить для меня новые и мало понятные вещи. Но с течением времени я начал чувствовать себя в этом уверенней, и даже смог предлагать какие-то свои изменения, которые опишу ниже в Code Review.

## Code Review

Я разобрался в коде, который написал @kanoplich для создания таблиц на Supabase.
Научился читать SQL код и понял, как работают RLS политики.
Заметил, что profiles в таблицах создаются поверх users, которые автоматически создаются Supabase.
Предложил команде использовать для id в профилях дефолтное значение auth.uid(), и с моим предложением согласились.
Также предложил отметить поле email как "Unique", чтобы email при регистрации всегда был уникальным.

Далее я проанализировал, как в `src/shared//api/auth/index.ts` работают функции для получения сессии, регистрации, входа в аккаунт и выхода из него.
Отметил, что везде используется проверка флага USE_MOCK_SUPABASE и при необходимости добавляется задержка при помощи функции delay().

После этого я разобрался, как в `src/shared//api/supabase-client.ts` работает функция по созданию клиента. Сама по себе функция простая, но мне впервые пришлось разобраться, для чего используются
переменные SUPABASE_URL и SUPABASE_ANON_KEY

- **Планы:**

В ближайшее время я планирую приступить к разработке Dashboard, а также внести некоторые правки в раобту AI-агента.

- **Затраченное время:** 4 часа

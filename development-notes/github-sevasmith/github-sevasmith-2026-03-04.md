# Дата: 2026-03-04

- **Что было сделано:**

Вчера мне удалось создать тестовое задание и топик на нашем удаленно сервере на Supabase. Отправил запрос с тестовым ответом на Грок, и получил первый полноценный ответ с оценкой.

Вечером у нас был созвон с командой. Во время созвона я установил себе Docker, чтобы иметь возможность запускать сервер локально. Также я создал файл .env.local, чтобы внести туда переменные для локальной работы сервера, куда мы занесли ключ от Грока. Поскольку этот .env файл находился только на моем ноутбуке и не передавался в репозитории, это было безопасно.

- **Проблемы, с которыми я столкнулся:**

При запуске Докера я столкнулся со следующей проблемой. При оправке сообщений Гроку приходила ошибка 400, затем ошибка 501. До этого мы отправляли при запуске Edge Functions дополнительный флаг, --no-verify-jwt, чтобы функция не проверяла JWT, но после того, как мы этот флаг убрали, ошибки исчезли и мы получили ответ от Грока.

- **Планы:**

В ближайшее время я планирую разобраться с тем, как настроить работу с системными промптами Грока.

- **Затраченное время:** 4 часа

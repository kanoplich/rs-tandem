# Дата: 2026-03-23

**Что было сделано:**

Сегодня доработала хук useProfileData для страницы профиля согласно комментариям ментора.

Убрала лишние состояния и производные данные (avgScore, stageProgress).
Исправила имена состояний и возвращаемых полей (loading → isLoading, topicProgress → progress) для единообразия с остальным проектом.
Централизовала импорты утилит через shared
Убрала лишний setIsLoading(true) и console.error, оставила только toast.error для отображения ошибок.
В результате хук теперь только загружает данные и отдаёт их компонентам без лишней логики.

**Затраченное время:**

50 минут

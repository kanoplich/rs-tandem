# Дата: 2026-02-17

- **Что было сделано:**

Сегодня работала над глобальными стилями проекта. Основная задача — настроить базовые CSS-переменные, цвета, шрифты.

Основные шаги:

1. Добавила глобальные переменные CSS (`:root`) для фона, текста, карточек, рингов, сайдбара и диаграмм и т.д.
2. Немного читала и смотрела видео про Tailwind.
3. Использовала `@layer base` для базовых стилей тегов (`h1`–`h6`, `p`, `a`) и глобальных элементов (`*`, `body`).
4. Проверила применение стилей на простых компонентах, чтобы убедиться, что цвета и отступы работают корректно.

- **Проблемы:**

Потребовалось разобраться с тем, как правильно писать `focus:ring` и `ring-color`, чтобы красиво подсвечивались интерактивные элементы (инпуты, сайдбар).

- **Затраченное время:** 4 часа
